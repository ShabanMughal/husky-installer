# Installation

Husky Installer can be used in multiple ways. Choose the method that best fits your workflow.

## Using npx (Recommended)

The **recommended** way to use Husky Installer is with `npx`. This requires no installation:

<PackageManagerTabs
  commands={{
    npm: 'npx husky-installer',
    pnpm: 'pnpm dlx husky-installer',
    yarn: 'yarn dlx husky-installer',
    bun: 'bunx husky-installer',
  }}
/>

This command will:

<Steps>
  <Step>
    ### Detect Package Manager Automatically detects npm, yarn, pnpm, or bun in
    your project.
  </Step>
  <Step>
    ### Interactive Setup Guides you through the configuration process with
    prompts.
  </Step>
  <Step>
    ### Install & Configure Installs and configures everything automatically.
  </Step>
</Steps>

## Global Installation

Install globally to use the CLI from any directory:

<PackageManagerTabs
  commands={{
    npm: 'npm install -g husky-installer',
    pnpm: 'pnpm add -g husky-installer',
    yarn: 'yarn global add husky-installer',
    bun: 'bun add -g husky-installer',
  }}
/>

After installation, run:

<PackageManagerTabs
  commands={{
    npm: 'husky-installer',
    pnpm: 'husky-installer',
    yarn: 'husky-installer',
    bun: 'husky-installer',
  }}
/>

## Requirements

Before installing, ensure you have:

<DocsTable>
  <DocsTableHeader>
    <DocsTableRow>
      <DocsTableHead>Requirement</DocsTableHead>
      <DocsTableHead>Description</DocsTableHead>
    </DocsTableRow>
  </DocsTableHeader>
  <DocsTableBody>
    <DocsTableRow>
      <DocsTableCell><strong>Node.js</strong></DocsTableCell>
      <DocsTableCell>Version >= 18.0.0</DocsTableCell>
    </DocsTableRow>
    <DocsTableRow>
      <DocsTableCell><strong>Git Repository</strong></DocsTableCell>
      <DocsTableCell>Initialized with <code>git init</code></DocsTableCell>
    </DocsTableRow>
    <DocsTableRow>
      <DocsTableCell><strong>package.json</strong></DocsTableCell>
      <DocsTableCell>Must exist in your project root</DocsTableCell>
    </DocsTableRow>
  </DocsTableBody>
</DocsTable>

<Accordions type="single" defaultValue="important-note">
  <Accordion title="Important Note" id="important-note">
    The installer requires an initialized git repository. If you haven't run{' '}
    <code>git init</code> yet, do that first.
  </Accordion>
</Accordions>
